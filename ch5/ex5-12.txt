Exercise 5-12. Extend `entab` and `detab` to accept the shorthand

    $ entab -m +n

to mean tab stops every n columns starting at column m. Choose convenient
(for the user) default behavior.


Solution 5-12.
Copied the entab.c and detab.c programs from Chapter 1, added command line
options for tab stop and tab starting column. Needs testing.